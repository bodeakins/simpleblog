<h1>Blog Posts</h1>
<% @post.each do |post| %>
<div class="well">
	<h3><%= post.title %></h3>
	<p><%= truncate(post.body, :length => 250) %></p>
	<%= link_to "Read More", post_path(post), :class => 'btn btn-default' %>
</div>
<% end %>
